[Unit]
Description=HOMESERVER Backup Timer - Daily automated backup scheduling
Requires=homeserver-backup.service

[Timer]
# Daily backup at 2 AM with randomization
OnCalendar=*-*-* 02:00:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
